<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OPASNET 4A - 장비관리 시스템</title>
    <link href="/node_modules/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/node_modules/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/public/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- 상단 헤더 -->
    <nav class="navbar navbar-dark bg-dark fixed-top header-bar">
        <div class="container-fluid">
            <div class="d-flex align-items-center">
                <button class="btn btn-dark me-2" id="sidebarToggle">
                    <i class="bi bi-list"></i>
                </button>
                <span class="navbar-brand mb-0 h6">
                    <i class="bi bi-hdd-network"></i> OPASNET 4A - 장비관리 시스템
                </span>
            </div>
            <div class="d-flex align-items-center text-white">
                <span class="me-3">
                    <i class="bi bi-person-circle"></i>
                    <%= user ? user.name : '' %> 님
                    <% if (user && user.role === 'admin') { %>
                        <span class="badge ms-1">관리자</span>
                    <% } %>
                </span>
                <a href="/logout" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-box-arrow-right"></i> 로그아웃
                </a>
            </div>
        </div>
    </nav>

    <!-- 사이드바 -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <h6 class="text-white mb-0">MAIN NAVIGATION</h6>
        </div>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link <%= currentPath.startsWith('/equipment') ? 'active' : '' %>" href="/equipment">
                    <i class="bi bi-hdd-rack"></i>
                    <span>장비현황</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPath.startsWith('/rental') ? 'active' : '' %>" href="/rental/history">
                    <i class="bi bi-journal-text"></i>
                    <span>대여기록</span>
                </a>
            </li>
            <% if (user && user.role === 'admin') { %>
            <li class="nav-item mt-2">
                <div class="sidebar-section-title">관리자</div>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPath.startsWith('/admin') ? 'active' : '' %>" href="/admin/users">
                    <i class="bi bi-people"></i>
                    <span>사용자 관리</span>
                </a>
            </li>
            <% } %>
        </ul>
    </div>

    <!-- 메인 콘텐츠 -->
    <div class="main-content" id="mainContent">
        <%- body %>
    </div>

    <script src="/node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/public/js/app.js"></script>
</body>
</html>
